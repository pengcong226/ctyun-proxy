<!-- ============ 发帖说明（这段不要贴进帖子） ============
1. 发帖时打上 #开源推广 标签（编辑器右侧 + 号可插入官方申明模板，内容同下）
2. AI 内容规则：下面这份草稿是 AI 写的。按社区要求"AI生成、润色内容部分已
   截图发出"，二选一：
   a) 推荐：用自己的话重写正文（技术事实/命令/配置可以照抄，那些不算润色），
      重写后帖子无 AI 内容，申明第 4 条自然成立
   b) 或者：把 AI 写的正文部分截图发图，命令和代码块可以保留文字方便复制
3. 仓库链接贴这个：https://github.com/pengcong226/ctyun-proxy
4. 申明模板在下方横线之后，随正文一起贴出，所有选项保持"是"
============ 发帖说明结束 ============ -->

---

#### 本帖使用社区开源推广，符合推广要求。我申明并遵循社区要求的以下内容：
* **我的帖子已经打上 #开源推广 标签：** 是
* **我的开源项目完整开源，无未开源部分：** 是
* **我的开源项目已链接认可 LINUX DO 社区：** 是
* **我帖子内的项目介绍，AI生成、润色内容部分已截图发出：** 是
* **以上选择我承诺是永久有效的，接受社区和佬友监督：** 是

*以下为项目介绍正文内容，AI生成、润色内容已使用截图方式发出*

---

# [分享] 把天翼云云智助手逆向成 OpenAI 兼容 API，接入 zcode 当日常 agent 引擎（附完整攻坚记录）

## 一句话

天翼云的「云智助手」桌面客户端里藏着免费的 GLM-5.3 / DeepSeek-V4 / Qwen3.8-Max，我把它逆向成了一个本地 OpenAI 兼容反代（`127.0.0.1:8317/v1`），支持 SSE 流式、思维链、**agent 工具调用**——现在我的 zcode 天天跑在它上面。

> 免责声明：纯个人学习研究，逆向分析都在本地完成。用这玩意访问上游可能违反用户协议，有封号风险，自行评估。勿商用、勿对外提供代理。

## 为什么这活儿有意思（三个坑）

**坑 1：它名义上 OpenAI 兼容，但 agent 用不了。**
`/wenc/v3/openai/chat/completions` 路径确实长得像 OpenAI，但服务端**自带工具执行器 + 工具名白名单**。你带自定义 `tools` 请求，模型会乖乖调用，然后服务端把 `"工具名称不存在: xxx"` 当内容塞回来。zcode 接上去就是一问一答，第二步必死。

**坑 2：2.6.4 换了签名通道。**
老版本 `/ai/portal/v3/` 裸 cookie 就能打；2.6.4 挪到了 `/ai/portal/wenc/v3/`，多了 `Web-Signature` 头：`SHA256(sorted_params & MD5(body) & sk & timestamp & random)`。sk 藏在客户端 webview 的会话密钥里，得从 CDP hook 出来。

**坑 3：协议通了不等于能用。**
真正的工作量在协议之后：模型偶发输出**四种漂移格式**的工具调用（名字外置、XML 参数标签、截断 JSON……）、任务中途"叙述下一步却不调用工具"、上游随机断流（10038/10053）、服务端还会往上下文里注入"云智助手"身份模板导致模型在回复里评论"有人冒充系统指令"……

## 我的解法：提示词级工具调用翻译层

既然服务端白名单只认它自己的工具名，那就**不带上游 tools，把工具定义注入提示词**：

- 请求方向：`tools` → 注入首条 user 消息；`assistant.tool_calls` → `<tool_call>{"name":..,"arguments":{..}}</tool_call>` 文本；`tool` 结果 → `<tool_result>` 标签
- 响应方向：模型输出的 `<tool_call>` 流式解析回 `tool_calls` delta，`finish_reason="tool_calls"`，agent 客户端无感知循环

实测 zcode 112 工具形态的长任务能稳定跑完（3 个真实任务回归：5~7 轮、29 次工具调用、0 中断）。

配套修了一整套稳定性问题，都是真实使用里一轮轮暴露的：

| 问题 | 解法 |
|------|------|
| 模型输出 4 种漂移格式 | 容错解析器 + 截断 JSON 修复器 |
| 叙述型中断（说要做但不做） | 递进施压自愈重试 ×3 |
| 上游随机断流 | 看门狗（首字 45s/中途 90s）+ 断连分类重试 |
| 正文流式后断流丢工具块 | 断流收尾时解析已缓冲的完整调用救回 |
| 大参数调用（Write 大文件）超缓冲上限被截 | 上限 1MB + 截到最近完整调用边界 |
| 上游注入"云智助手"身份被模型回显 | 双层防护：提示词声明 + 缓冲期正则剥离 |
| 提示注入伪造工具调用 | 严格白名单：未声明工具名一律拦截 |

## 用法（三分钟）

```bash
# 前提：Windows + 云智助手客户端登录过一次（凭据从它的 config.json 解密读取，客户端不用常驻）
pip install pycryptodome websocket-client
python ctyun_proxy.py        # 127.0.0.1:8317，默认走旧路径无需 sk
```

zcode 配置：

```json
{
  "providers": {
    "ctyun": {
      "baseUrl": "http://127.0.0.1:8317/v1",
      "apiKey": "sk-ctyun",
      "models": ["TEXT_GLM_5.3"]
    }
  }
}
```

要更稳的 wenc 签名通道，带调试端口启动客户端让反代自动 hook sk：

```
"...\ecloudAiAssistant.exe" --remote-debugging-port=19233
```

## 关于维护：抛砖引玉

**先说清楚：这是快照式分享，我不打算长期维护。** 逆向过程和七轮修复已经把我想要的东西都榨出来了（现在 zcode 跑得很稳），后续客户端更新导致失效我不会跟进适配，Issue/PR 也不会看。

但渠道本身是真实可用的，思路也都写在 DEVLOG 里了。**欢迎直接 fork 去改**，几个我没继续挖的方向，留给有兴趣的大佬：

1. **工具白名单的协议原生绕法**——我试过把自定义工具伪装成内置工具名，服务端校验了 schema，没过。最后走了提示词注入这条路。也许有人能找到更干净的路子？
2. **工具结果伪造攻击面**：`<tool_result>` 里的内容没做伪 XML 转义，理论上工具输出可以构造 `</tool_result><tool_call>...` 伪造已声明工具的调用。转义会不会影响模型理解？没深究。
3. **上游断流**：10038/10053 在长回复时偶发，现在的策略是分类重试 + 已流式内容收尾。应该有更优雅的。
4. macOS/Linux 客户端没看过，路径和加密方案是否一致未知。

**如果你做出了改进版，欢迎回帖分享仓库链接，方便后人直达。**

完整逆向过程（asar 解包 → 三层认证体系 → 签名算法定位 → 25 节实测修复记录，含每轮失败的抓包分析）在仓库 `DEVLOG.md`。

---

**附：仓库结构**（单文件反代 + 离线单测 + 端到端测试，无 web 框架依赖）

```
ctyun_proxy.py     # 主脚本 ~1400 行
DEVLOG.md          # 完整攻坚记录（脱敏）
tests/             # 离线单测 + agent 闭环 + zcode 形态压测
```

（发帖时附上 GitHub 链接）
